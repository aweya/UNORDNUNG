function prov(){

    this.draw=function(){
        textSize(width / 30);
    textAlign(CENTER, CENTER);
    textFont('monospace');
        background(50);
        text("Bewege und drücke die        um zu spielen", width / 2, height / 2);
    }
}